# Class Notes - Session 13 - 3/12

## Going over the Quiz: 
- considered mobile-first design because we directly label it, it states the screen side
- when it hovers over the cardm it takes 0.3seconds to change the color, there's transitions 
- never USE ChatGPT or CoPilot for your HTML/CSS files 

## CSS Frameworks: 
    - Bootstap 
    - Tailwind CSS -> directly uses the class to write out what the fonts/size/etc. ; don't need a CSS file 
    - Vanilla: would need a CSS file 
    - UI Shadcn

## JS Questions/Answer: 
- console.log: this is for developer purpose and if they need to see their products 
- addEventListener: the element keep monitoring all it's movements (event: how you expect your users to interact with a page)
- DOMContentLoaded: means when the page is loading then x,y,z will happen
- logic if: the logic is at the bottom, so the EventListener will keep notice and then it will strike and do the logic behind it
- {} to define the scope
- () it goes to the function, there's an attachment, parameters
- [] related to an array, just to store multiple values 
- const: to define a constant 
- ElementByID(""): if the element has the id of "", then do x,y,z --> uses the id to match and properly execute 



