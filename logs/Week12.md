# Week 11 [4/12-4/18]

## What I learned this week
- This week was tailored towards API. I learned that async and await are ways in which we can ensure that we get the data that we need. It's a simply yet effective way to notify end users to simply wait for the data. Additionally, I learned that some API's require an API key and if you were to push this onto GitHbu, anyone can then steal your key. It's important to use gitignore when dealing with sensitive API's.

- I learned that each image pixel has a color value and it was through the tiger website in which I got to learn more about how to manipulate the pixels. I found it cool that an image data is essentially an array when you think about it. 


## Code I'm Proud of 
- The tiger file code was byfar one of my favorites. This entire semester we have been manipulating texts, headers, placements, etc. however, we haven't dwell much into images themselves. While you can change the format of the image, you can even take it a step forward and change the color and density of it. It was like doing all the camera editing but through code which was nice. I thought it was a cool trick to know. 

## Challenges I faced
- After taking the quiz, I still don't quite understand the sudtle difference between let = 0 and let = 1. I thought I did but when answering the questions, I kept going in circles. I am working on inputting the code that was given to us onto this file so I can properly see the changes. 


## Mini Project 2: 
- What made this project a bit difficult was that one path can have different endings. I had to really understand if and else statements as I knew this would help me out. For example, if I was starting off at South Station and chose to go to North Station, then it will generate specific t line information. There were many different paths that one can take so having to upload the links and ensuring that all the if else statements were working made my website successful. 
- The hardest thing to implement was the "attractions" section as there could be a million of outcomes, some with similar outcomes, and so having to get rid of any repetitive paths was crucial. At first I noticed that when two paths couldn't generate a response, it was simply there, it gave me no signal. And so now when a path can't ge figured out, it notifies the end users and asks them to pick your location/destination again. I felt as if that simple notification was messing with the goal of the webiste. 


