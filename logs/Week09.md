# Week 9 [3/23-3/27]

## What I learned this week
- The most important thing I learned was that you have to incorporate a script section at the end of body for JavaScript. It is recommended that JavaScript is at the bottom of the HTML file. 
- I learned what addEventlistener does to the file. Events are actions that happen on the page. It tells Java to listen and read the event and when something happens to do that exact thing. I did not realize how often I am doing some level of action on any websites. 
- When learning JavaScript, the usage of console has been helpful (going on a webpage and using inspect). Console has been extremely helpful on running a few lines of code and seeing the output. It is worth noting that console is only temporary.
- Also I learned the difference between "let" and "const" which is something that is truly important. One uses let for when they think the values will change and one uses cost for values that they think will stay the same. 

## Code I'm Proud of 
- I am proud of being able to call HTML through JavaScript. In the previous weeks, I wasn't quite sure how JavaScript was effecting HTML soley because there were lots of text and variables into play. However, doing the session 16 demo, it allowed me to see how both languages were being used. Building a very simple, basic website allowed me to see which functions were being used. In my example, I can see that document.querySelector(x) was being used as a way to call out HTML. 

## Challenges I faced
- I think where I am having trouble is how let and const interact. Seeing the session 17 demo, I can see why some lines were let and others were const. I think it's more so of seeing the bigger picture. I believe that individually I understand what is going on but when it's all together because I am viewing everything, I feel a bit overwhelmed. 

## AI Usage (if any)





